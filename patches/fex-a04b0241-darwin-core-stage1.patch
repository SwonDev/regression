diff --git a/CMakeLists.txt b/CMakeLists.txt
index 92d86a7..7c11527 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -36,6 +36,7 @@ set_property(CACHE FEXCORE_PROFILER_BACKEND PROPERTY STRINGS gpuvis tracy)
 option(ENABLE_GLIBC_ALLOCATOR_HOOK_FAULT "Enables glibc memory allocation hooking with fault for CI testing")
 option(USE_PDB_DEBUGINFO "Build debug info in PDB format" FALSE)
 option(BUILD_STEAM_SUPPORT "Enable Steam integration" FALSE)
+option(BUILD_DARWIN_CORE_ONLY "Build only FEXCore for the Regression Darwin feasibility probe" FALSE)
 
 set(X86_32_TOOLCHAIN_FILE "${CMAKE_CURRENT_SOURCE_DIR}/Data/CMake/toolchain_x86_32.cmake" CACHE FILEPATH "Toolchain file for the (cross-)compiler targeting i686")
 set(X86_64_TOOLCHAIN_FILE "${CMAKE_CURRENT_SOURCE_DIR}/Data/CMake/toolchain_x86_64.cmake" CACHE FILEPATH "Toolchain file for the (cross-)compiler targeting x86_64")
@@ -61,7 +62,8 @@ if (NOT CMAKE_SIZEOF_VOID_P EQUAL 8)
   message(FATAL_ERROR "Unsupported pointer size ${CMAKE_SIZEOF_VOID_P}."
     " FEX only supports 64-bit (8-byte pointer) systems."
     " If you believe this is in error, file an issue.")
-elseif (NOT (WIN32 OR CMAKE_SYSTEM_NAME STREQUAL "Linux"))
+elseif (NOT (WIN32 OR CMAKE_SYSTEM_NAME STREQUAL "Linux" OR
+             (APPLE AND BUILD_DARWIN_CORE_ONLY)))
   message(FATAL_ERROR "Unsupported system type ${CMAKE_SYSTEM_NAME}."
     " FEX only supports Linux and Windows."
     " If you believe this is in error, file an issue.")
@@ -594,6 +596,11 @@ add_subdirectory(FEXHeaderUtils/)
 add_subdirectory(CodeEmitter/)
 add_subdirectory(FEXCore/)
 
+if (BUILD_DARWIN_CORE_ONLY)
+  message(STATUS "Regression probe: configured FEXCore-only Darwin build")
+  return()
+endif()
+
 if (ARCHITECTURE_arm64 AND NOT MINGW AND NOT BUILD_STEAM_SUPPORT)
   # Binfmt_misc files must be installed prior to Source/ installs
   add_subdirectory(Data/binfmts/)
diff --git a/FEXCore/Source/CMakeLists.txt b/FEXCore/Source/CMakeLists.txt
index 850a96e..a4e6db4 100644
--- a/FEXCore/Source/CMakeLists.txt
+++ b/FEXCore/Source/CMakeLists.txt
@@ -115,13 +115,13 @@ if (ENABLE_ZYDIS)
   list(APPEND LIBS Zydis::Zydis)
 endif()
 
-if (NOT MINGW)
-  list(APPEND LIBS dl)
-else()
+if (MINGW)
   list(APPEND LIBS synchronization)
   if (ARCHITECTURE_arm64ec)
     list(APPEND LIBS mincore)
   endif()
+elseif (NOT APPLE)
+  list(APPEND LIBS dl)
 endif()
 
 if (CMAKE_CXX_COMPILER_ID STREQUAL "GNU")
@@ -278,7 +278,7 @@ function(AddLibrary Name Type)
 
   # During generation of the import library (dll.a), MinGW needs some extra symbols from libraries
   # such as fmt, which are propagated by FEXCore_Base. Wonderful.
-  if (MINGW)
+  if (MINGW OR APPLE)
     target_link_libraries(${Name} PRIVATE FEXCore_Base)
   endif()
   AddDefaultOptionsToTarget(${Name})
diff --git a/FEXCore/Source/Interface/Core/CPUBackend.cpp b/FEXCore/Source/Interface/Core/CPUBackend.cpp
index 1b1fc10..2f20339 100644
--- a/FEXCore/Source/Interface/Core/CPUBackend.cpp
+++ b/FEXCore/Source/Interface/Core/CPUBackend.cpp
@@ -11,7 +11,7 @@
 
 #include <cstdint>
 
-#ifndef _WIN32
+#if !defined(_WIN32) && !defined(__APPLE__)
 #include <sys/prctl.h>
 #endif
 
@@ -373,7 +373,7 @@ namespace CPU {
   }
 
   auto CodeBufferManager::AllocateNew(size_t Size) -> fextl::shared_ptr<CodeBuffer> {
-#ifndef _WIN32
+#if !defined(_WIN32) && !defined(__APPLE__)
 // MDWE (Memory-Deny-Write-Execute) is a new Linux 6.3 feature.
 // It's equivalent to systemd's `MemoryDenyWriteExecute` but implemented entirely in the kernel.
 //
diff --git a/FEXCore/Source/Interface/Core/Interpreter/Fallbacks/F80Fallbacks.h b/FEXCore/Source/Interface/Core/Interpreter/Fallbacks/F80Fallbacks.h
index e37e116..229e53c 100644
--- a/FEXCore/Source/Interface/Core/Interpreter/Fallbacks/F80Fallbacks.h
+++ b/FEXCore/Source/Interface/Core/Interpreter/Fallbacks/F80Fallbacks.h
@@ -359,7 +359,7 @@ struct OpHandlers<IR::OP_F64SINCOS> {
   FEXCORE_PRESERVE_ALL_ATTR static VectorScalarF64Pair handle(double src, FEXCore::Core::CpuStateFrame* Frame) {
     FEXCORE_PROFILE_INSTANT_INCREMENT(Frame->Thread, AccumulatedFloatFallbackCount, 1);
     double sin, cos;
-#ifdef _WIN32
+#if defined(_WIN32) || defined(__APPLE__)
     sin = ::sin(src);
     cos = ::cos(src);
 #else
diff --git a/FEXCore/Source/Interface/Core/JIT/MiscOps.cpp b/FEXCore/Source/Interface/Core/JIT/MiscOps.cpp
index ff8e916..d18d378 100644
--- a/FEXCore/Source/Interface/Core/JIT/MiscOps.cpp
+++ b/FEXCore/Source/Interface/Core/JIT/MiscOps.cpp
@@ -5,7 +5,7 @@ tags: backend|arm64
 $end_info$
 */
 
-#ifndef _WIN32
+#if !defined(_WIN32) && !defined(__APPLE__)
 #include <syscall.h>
 #endif
 
@@ -17,6 +17,18 @@ $end_info$
 #include <FEXCore/Debug/InternalThreadState.h>
 #include <FEXCore/Utils/EnumUtils.h>
 
+#ifdef __APPLE__
+#include <pthread.h>
+
+static uint64_t DarwinProcessorID() {
+  size_t CPU {};
+  if (::pthread_cpu_number_np(&CPU) != 0) {
+    return 0;
+  }
+  return CPU;
+}
+#endif
+
 namespace FEXCore::CPU {
 
 DEF_OP(WFET) {
@@ -234,7 +246,25 @@ DEF_OP(ProcessorID) {
     mrs(GetReg(Node), ARMEmitter::SystemRegister::TPIDRRO_EL0);
     return;
   }
-#ifdef _WIN32
+#ifdef __APPLE__
+  PushDynamicRegs(TMP2);
+  SpillStaticRegs(TMP2);
+
+  LoadConstant(ARMEmitter::Size::i64Bit, ARMEmitter::Reg::r1, reinterpret_cast<uintptr_t>(&DarwinProcessorID));
+  if (!CTX->Config.DisableVixlIndirectCalls) [[unlikely]] {
+    GenerateIndirectRuntimeCall<uint64_t>(ARMEmitter::Reg::r1);
+  } else {
+    blr(ARMEmitter::Reg::r1);
+  }
+
+  if (!TMP_ABIARGS) {
+    mov(ARMEmitter::Size::i64Bit, TMP1, ARMEmitter::Reg::r0);
+  }
+  FillStaticRegs();
+  PopDynamicRegs();
+  mov(ARMEmitter::Size::i64Bit, GetReg(Node), TMP1);
+  return;
+#elif defined(_WIN32)
   else {
     // If on Windows and TPIDRRO isn't supported (like in wine), then this is a programming error.
     ERROR_AND_DIE_FMT("Unsupported");
diff --git a/FEXCore/Source/Utils/Allocator.cpp b/FEXCore/Source/Utils/Allocator.cpp
index 3d9944a..5708246 100644
--- a/FEXCore/Source/Utils/Allocator.cpp
+++ b/FEXCore/Source/Utils/Allocator.cpp
@@ -23,8 +23,10 @@
 #include <fcntl.h>
 #ifndef _WIN32
 #include <sys/mman.h>
+#ifndef __APPLE__
 #include <sys/user.h>
 #endif
+#endif
 
 namespace fextl::pmr {
 static fextl::pmr::default_resource FEXDefaultResource;
@@ -60,6 +62,11 @@ void* FEX_mmap(void* addr, size_t length, int prot, int flags, int fd, off_t off
 }
 
 void VirtualName(const char* Name, void* Ptr, size_t Size) {
+#ifdef __APPLE__
+  (void)Name;
+  (void)Ptr;
+  (void)Size;
+#else
   static bool Supports {true};
   if (Supports) {
     auto Result = prctl(PR_SET_VMA, PR_SET_VMA_ANON_NAME, Ptr, Size, Name);
@@ -68,6 +75,7 @@ void VirtualName(const char* Name, void* Ptr, size_t Size) {
       Supports = false;
     }
   }
+#endif
 }
 
 int FEX_munmap(void* addr, size_t length) {
@@ -262,17 +270,23 @@ fextl::vector<MemoryRegion> StealMemoryRegion(uintptr_t Begin, uintptr_t End) {
   }
 
   // Block remaining memory gaps
+#ifdef __APPLE__
+  bool SupportsDontDump = false;
+#else
   bool SupportsDontDump = true;
+#endif
   for (auto RegionIt = Regions.begin(); RegionIt != Regions.end(); ++RegionIt) {
     auto Alloc = ::mmap(RegionIt->Ptr, RegionIt->Size, PROT_NONE, MAP_ANONYMOUS | MAP_NORESERVE | MAP_PRIVATE | MAP_FIXED_NOREPLACE, -1, 0);
 
     if (SupportsDontDump) {
+#ifndef __APPLE__
       // Mark these regions as don't dump so that coredump doesn't try dumping large unmapped regions.
       // Ideally coredump would be smart enough to only dump resident pages, but here we are.
       auto Result = madvise(RegionIt->Ptr, RegionIt->Size, MADV_DONTDUMP);
       if (Result == -1) {
         SupportsDontDump = false;
       }
+#endif
     }
 
     LogMan::Throw::AFmt(Alloc != MAP_FAILED, "StealMemoryRegion: mmap({}, {:x}) failed: {}", fmt::ptr(RegionIt->Ptr), RegionIt->Size, errno);
diff --git a/FEXCore/Source/Utils/Allocator/64BitAllocator.cpp b/FEXCore/Source/Utils/Allocator/64BitAllocator.cpp
index 74515ef..6ab3c00 100644
--- a/FEXCore/Source/Utils/Allocator/64BitAllocator.cpp
+++ b/FEXCore/Source/Utils/Allocator/64BitAllocator.cpp
@@ -26,7 +26,9 @@
 #include <new>
 #include <sys/mman.h>
 #include <sys/utsname.h>
+#ifndef __APPLE__
 #include <sys/user.h>
+#endif
 #include <type_traits>
 #include <utility>
 
@@ -508,7 +510,9 @@ void OSAllocator_64Bit::AllocateMemoryRegions(fextl::vector<FEXCore::Allocator::
     mprotect(it.Ptr, ObjectAllocSize, PROT_READ | PROT_WRITE);
 
     // This enables the kernel to use transparent large pages in the allocator which can reduce memory pressure
+#ifndef __APPLE__
     ::madvise(it.Ptr, ObjectAllocSize, MADV_HUGEPAGE);
+#endif
 
     FEXCore::Allocator::VirtualName("FEXMem_Misc", reinterpret_cast<void*>(it.Ptr), ObjectAllocSize);
 
diff --git a/FEXCore/Source/Utils/AllocatorHooks.cpp b/FEXCore/Source/Utils/AllocatorHooks.cpp
index fd6947f..7174632 100644
--- a/FEXCore/Source/Utils/AllocatorHooks.cpp
+++ b/FEXCore/Source/Utils/AllocatorHooks.cpp
@@ -11,7 +11,11 @@
 #endif
 
 #include <cstdint>
+#ifdef __APPLE__
+#include <malloc/malloc.h>
+#else
 #include <malloc.h>
+#endif
 #include <stdlib.h>
 #include <stdio.h>
 #include <string.h>
@@ -109,8 +113,10 @@ static void* FEX_rp_mmap(size_t size, size_t alignment, size_t* offset, size_t*
 #endif
     prctl(PR_SET_VMA, PR_SET_VMA_ANON_NAME, ptr, map_size, global_config.page_name);
 
-    // Disable HUGEPAGE on allocation from rpmalloc.
+    // Disable HUGEPAGE on allocation from rpmalloc where the host supports it.
+#ifndef __APPLE__
     madvise(ptr, map_size, MADV_NOHUGEPAGE);
+#endif
   }
 
   if (ptr == nullptr) {
@@ -188,7 +194,12 @@ void* calloc(size_t n, size_t size) {
   return ::calloc(n, size);
 }
 void* memalign(size_t align, size_t s) {
+#ifdef __APPLE__
+  void* ptr {};
+  return ::posix_memalign(&ptr, align, s) == 0 ? ptr : nullptr;
+#else
   return ::memalign(align, s);
+#endif
 }
 void* valloc(size_t size) {
   return ::valloc(size);
@@ -203,8 +214,21 @@ void free(void* ptr) {
   return ::free(ptr);
 }
 size_t malloc_usable_size(void* ptr) {
+#ifdef __APPLE__
+  return ::malloc_size(ptr);
+#else
   return ::malloc_usable_size(ptr);
+#endif
 }
 void* aligned_alloc(size_t a, size_t s) {
+#ifdef __APPLE__
+  // posix_memalign on Darwin rejects alignments smaller than a pointer even
+  // though fextl legitimately requests alignof(uint32_t). Normalize only the
+  // host allocator contract; the requested object alignment is preserved.
+  const size_t Alignment = a < alignof(void*) ? alignof(void*) : a;
+  void* ptr {};
+  return ::posix_memalign(&ptr, Alignment, s) == 0 ? ptr : nullptr;
+#else
   return ::aligned_alloc(a, s);
+#endif
 }
diff --git a/FEXCore/Source/Utils/Profiler.cpp b/FEXCore/Source/Utils/Profiler.cpp
index 67fe0ca..84ff0fd 100644
--- a/FEXCore/Source/Utils/Profiler.cpp
+++ b/FEXCore/Source/Utils/Profiler.cpp
@@ -1,10 +1,12 @@
 // SPDX-License-Identifier: MIT
 #include <cstdint>
 #include <fcntl.h>
-#ifndef _WIN32
+#if !defined(_WIN32) && !defined(__APPLE__)
 #include <linux/magic.h>
 #include <sys/stat.h>
 #include <sys/vfs.h>
+#elif defined(__APPLE__)
+#include <sys/stat.h>
 #endif
 
 #include <FEXCore/Utils/LogManager.h>
diff --git a/FEXCore/include/FEXCore/Utils/AllocatorHooks.h b/FEXCore/include/FEXCore/Utils/AllocatorHooks.h
index 48bbe5e..5ea6e1e 100644
--- a/FEXCore/include/FEXCore/Utils/AllocatorHooks.h
+++ b/FEXCore/include/FEXCore/Utils/AllocatorHooks.h
@@ -6,7 +6,11 @@
 
 #ifndef _WIN32
 #include <stdlib.h>
+#ifdef __APPLE__
+#include <malloc/malloc.h>
+#else
 #include <malloc.h>
+#endif
 #include <sys/mman.h>
 #else
 #define NTDDI_VERSION 0x0A000005
@@ -142,7 +146,15 @@ inline bool VirtualProtect(void* Ptr, size_t Size, ProtectOptions options) {
 }
 
 inline void VirtualTHPControl(const void* Ptr, size_t Size, THPControl Control) {
+#ifdef __APPLE__
+  // Darwin has no transparent-huge-page madvise controls. FEX only uses this
+  // as a host-side performance hint, so preserving the mapping is correct.
+  (void)Ptr;
+  (void)Size;
+  (void)Control;
+#else
   ::madvise(const_cast<void*>(Ptr), Size, Control == THPControl::Enable ? MADV_HUGEPAGE : MADV_NOHUGEPAGE);
+#endif
 }
 
 #endif
diff --git a/FEXCore/include/FEXCore/Utils/PrctlUtils.h b/FEXCore/include/FEXCore/Utils/PrctlUtils.h
index 4494ce2..6050e62 100644
--- a/FEXCore/include/FEXCore/Utils/PrctlUtils.h
+++ b/FEXCore/include/FEXCore/Utils/PrctlUtils.h
@@ -1,7 +1,7 @@
 // SPDX-License-Identifier: MIT
 #pragma once
 
-#ifndef _WIN32
+#if !defined(_WIN32) && !defined(__APPLE__)
 #include <sys/mman.h>
 #include <sys/user.h>
 #include <sys/prctl.h>
@@ -50,4 +50,4 @@
 #define PR_SHADOW_STACK_ENABLE (1ULL << 0)
 #endif
 
-#endif // ifndef _WIN32
+#endif // !defined(_WIN32) && !defined(__APPLE__)
diff --git a/FEXCore/include/FEXCore/Utils/SignalScopeGuards.h b/FEXCore/include/FEXCore/Utils/SignalScopeGuards.h
index d4ceeab..8da5cee 100644
--- a/FEXCore/include/FEXCore/Utils/SignalScopeGuards.h
+++ b/FEXCore/include/FEXCore/Utils/SignalScopeGuards.h
@@ -8,7 +8,7 @@
 #include <mutex>
 #include <optional>
 #include <signal.h>
-#ifndef _WIN32
+#if !defined(_WIN32) && !defined(__APPLE__)
 #include <sys/syscall.h>
 #endif
 #include <unistd.h>
@@ -153,10 +153,27 @@ private:
 class ScopedSignalMasker final {
 public:
   explicit ScopedSignalMasker(uint64_t Mask)
+#ifdef __APPLE__
+    : OriginalMask() {
+    sigset_t NewMask {};
+    sigemptyset(&NewMask);
+    for (int Signal = 1; Signal < NSIG && Signal <= 64; ++Signal) {
+      if ((Mask & (1ULL << (Signal - 1))) != 0) {
+        sigaddset(&NewMask, Signal);
+      }
+    }
+
+    sigset_t PreviousMask {};
+    const int Result = ::pthread_sigmask(SIG_SETMASK, &NewMask, &PreviousMask);
+    LOGMAN_THROW_A_FMT(Result == 0, "{} failed to mask signals with {}", __func__, Result);
+    OriginalMask = PreviousMask;
+  }
+#else
     : OriginalMask(0) {
     // Mask all signals, storing the original incoming mask
     ::syscall(SYS_rt_sigprocmask, SIG_SETMASK, &Mask, &*OriginalMask, sizeof(*OriginalMask));
   }
+#endif
 
   // Move-only type
   ScopedSignalMasker(const ScopedSignalMasker&) = delete;
@@ -168,11 +185,20 @@ public:
 
   ~ScopedSignalMasker() {
     if (OriginalMask) {
+#ifdef __APPLE__
+      const int Result = ::pthread_sigmask(SIG_SETMASK, &*OriginalMask, nullptr);
+      LOGMAN_THROW_A_FMT(Result == 0, "{} failed to restore signal mask with {}", __func__, Result);
+#else
       ::syscall(SYS_rt_sigprocmask, SIG_SETMASK, &OriginalMask, nullptr, sizeof(*OriginalMask));
+#endif
     }
   }
 private:
+#ifdef __APPLE__
+  std::optional<sigset_t> OriginalMask {};
+#else
   std::optional<uint64_t> OriginalMask {};
+#endif
 };
 
 /**
diff --git a/FEXCore/include/FEXCore/Utils/SpinWaitLock.h b/FEXCore/include/FEXCore/Utils/SpinWaitLock.h
index 1fa0c03..5a6fdd9 100644
--- a/FEXCore/include/FEXCore/Utils/SpinWaitLock.h
+++ b/FEXCore/include/FEXCore/Utils/SpinWaitLock.h
@@ -3,6 +3,7 @@
 
 #include <atomic>
 #include <chrono>
+#include <climits>
 #include <mutex>
 #include <type_traits>
 
@@ -101,6 +102,18 @@ static inline uint64_t LoadExclusive(uint64_t* Futex) {
   return Result;
 }
 
+#if defined(__APPLE__) && ULONG_MAX != ULLONG_MAX
+#error "Unexpected Darwin integer model"
+#elif defined(__APPLE__)
+// Darwin defines uint64_t as unsigned long long while size_t is unsigned long.
+// Both are 64-bit operands for the AArch64 exclusive-load instruction.
+static inline unsigned long LoadExclusive(unsigned long* Futex) {
+  unsigned long Result {};
+  __asm volatile(SPINLOOP_WFE_LDX_64BIT : [Result] "=r"(Result), [Futex] "+r"(Futex)::"memory");
+  return Result;
+}
+#endif
+
 static inline uint8_t WFELoadAtomic(uint8_t* Futex) {
   uint8_t Result {};
   __asm volatile(SPINLOOP_8BIT : [Result] "=r"(Result), [Futex] "+r"(Futex)::"memory");
@@ -129,6 +142,14 @@ static inline uint64_t WFELoadAtomic(uint64_t* Futex) {
   return Result;
 }
 
+#ifdef __APPLE__
+static inline unsigned long WFELoadAtomic(unsigned long* Futex) {
+  unsigned long Result {};
+  __asm volatile(SPINLOOP_64BIT : [Result] "=r"(Result), [Futex] "+r"(Futex)::"memory");
+  return Result;
+}
+#endif
+
 template<typename Pred, typename T>
 static inline void WaitPred(T* Futex, T ComparisonValue) {
   auto AtomicFutex = std::atomic_ref<T>(*Futex);
diff --git a/FEXCore/include/FEXCore/Utils/WritePriorityMutex.h b/FEXCore/include/FEXCore/Utils/WritePriorityMutex.h
index 84742f8..9916981 100644
--- a/FEXCore/include/FEXCore/Utils/WritePriorityMutex.h
+++ b/FEXCore/include/FEXCore/Utils/WritePriorityMutex.h
@@ -3,11 +3,11 @@
 #include <atomic>
 #include <cstdint>
 
-#if !defined(_WIN32)
+#if !defined(_WIN32) && !defined(__APPLE__)
 #include <linux/futex.h> /* Definition of FUTEX_* constants */
 #include <sys/syscall.h> /* Definition of SYS_* constants */
 #include <unistd.h>
-#else
+#elif defined(_WIN32)
 #include <synchapi.h>
 // Don't pull in all WIN32 headers for INFINITE. Causes too many problems.
 #ifndef INFINITE
@@ -270,7 +270,23 @@ public:
 
 private:
 
-#if !defined(_WIN32)
+#if defined(__APPLE__)
+  void FutexWaitForWriteAvailable(uint32_t Expected) {
+    std::atomic_ref<uint32_t>(Futex).wait(Expected, std::memory_order_relaxed);
+  }
+
+  void FutexWaitForReadAvailable(uint32_t Expected) {
+    std::atomic_ref<uint32_t>(Futex).wait(Expected, std::memory_order_relaxed);
+  }
+
+  void FutexWakeWriter() {
+    std::atomic_ref<uint32_t>(Futex).notify_one();
+  }
+
+  void FutexWakeReaders() {
+    std::atomic_ref<uint32_t>(Futex).notify_all();
+  }
+#elif !defined(_WIN32)
   void FutexWaitForWriteAvailable(uint32_t Expected) {
     ::syscall(SYS_futex, &Futex, FUTEX_PRIVATE_FLAG | FUTEX_WAIT_BITSET, Expected, nullptr, nullptr, FUTEX_BITSET_WAIT_WRITERS);
   }
diff --git a/FEXHeaderUtils/FEXHeaderUtils/Filesystem.h b/FEXHeaderUtils/FEXHeaderUtils/Filesystem.h
index 44c784d..d6d6f3c 100644
--- a/FEXHeaderUtils/FEXHeaderUtils/Filesystem.h
+++ b/FEXHeaderUtils/FEXHeaderUtils/Filesystem.h
@@ -9,7 +9,10 @@
 #include <fcntl.h>
 #include <memory_resource>
 #include <string_view>
-#ifndef _WIN32
+#ifdef __APPLE__
+#include <copyfile.h>
+#include <limits.h>
+#elif !defined(_WIN32)
 #include <linux/limits.h>
 #include <sys/sendfile.h>
 #else
@@ -216,7 +219,11 @@ inline bool CopyFile(const fextl::string& From, const fextl::string& To, CopyOpt
       close(SourceFD);
       return false;
     }
+#ifdef __APPLE__
+    bool Result = fcopyfile(SourceFD, DestinationFD, nullptr, COPYFILE_DATA) == 0;
+#else
     bool Result = sendfile(DestinationFD, SourceFD, nullptr, buf.st_size) == buf.st_size;
+#endif
     close(DestinationFD);
     close(SourceFD);
     return Result;
diff --git a/FEXHeaderUtils/FEXHeaderUtils/Syscalls.h b/FEXHeaderUtils/FEXHeaderUtils/Syscalls.h
index e5c2824..76c7ad4 100644
--- a/FEXHeaderUtils/FEXHeaderUtils/Syscalls.h
+++ b/FEXHeaderUtils/FEXHeaderUtils/Syscalls.h
@@ -4,10 +4,14 @@
 #include <FEXCore/Utils/LogManager.h>
 
 #include <cstdint>
+#include <cerrno>
+#include <pthread.h>
 #include <sched.h>
 #include <signal.h>
 #include <stdio.h>
-#ifndef _WIN32
+#if defined(__APPLE__)
+#include <sys/syscall.h>
+#elif !defined(_WIN32)
 #include <syscall.h>
 #else
 #include <processthreadsapi.h>
@@ -36,7 +40,7 @@ namespace FHU::Syscalls {
 #define CLONE_PIDFD 0x00001000
 #endif
 
-#if defined(__aarch64__) || defined(_M_ARM64)
+#if !defined(__APPLE__) && (defined(__aarch64__) || defined(_M_ARM64))
 #ifndef SYS_statx
 #define SYS_statx 291
 #endif
@@ -51,7 +55,56 @@ namespace FHU::Syscalls {
 #define SYS_pidfd_open 434
 #endif
 
-#ifndef _WIN32
+#if defined(__APPLE__)
+inline int32_t getcpu(uint32_t* cpu, uint32_t* node) {
+  size_t CPU {};
+  const int Result = ::pthread_cpu_number_np(&CPU);
+  if (Result != 0) {
+    errno = Result;
+    return -1;
+  }
+  if (cpu) {
+    *cpu = static_cast<uint32_t>(CPU);
+  }
+  if (node) {
+    *node = 0;
+  }
+  return 0;
+}
+
+inline int32_t gettid() {
+  uint64_t ThreadID {};
+  const int Result = ::pthread_threadid_np(nullptr, &ThreadID);
+  if (Result != 0) {
+    errno = Result;
+    return -1;
+  }
+  return static_cast<int32_t>(ThreadID);
+}
+
+inline int32_t tgkill(pid_t, pid_t, int) {
+  errno = ENOTSUP;
+  return -1;
+}
+
+inline int32_t statx(int, const char*, int32_t, uint32_t, void*) {
+  errno = ENOTSUP;
+  return -1;
+}
+
+inline int32_t renameat2(int olddirfd, const char* oldpath, int newdirfd, const char* newpath, unsigned int flags) {
+  if (flags == 0) {
+    return ::renameat(olddirfd, oldpath, newdirfd, newpath);
+  }
+  errno = ENOTSUP;
+  return -1;
+}
+
+inline int32_t pidfd_open(pid_t, unsigned int) {
+  errno = ENOTSUP;
+  return -1;
+}
+#elif !defined(_WIN32)
 inline int32_t getcpu(uint32_t* cpu, uint32_t* node) {
   // Third argument is unused
 #if defined(HAS_SYSCALL_GETCPU) && HAS_SYSCALL_GETCPU
